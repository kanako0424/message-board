<%= form_with(model: message, local: true) do |f| %>
  <%= f.label :content, 'メッセージ' %>
  <%= f.text_field :content %>

  <%= f.submit '投稿' %>
<% end %>

<!-- パーシャルの汎用性を高めるために、messageは@messageにしない。
インスタンス変数じゃなくても使えるようになるため -->
